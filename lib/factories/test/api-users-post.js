// Generated by CoffeeScript 1.9.3
module.exports.testApiUsersPostForbidden = function(command_serve, pgDropCreateMigrate, shutdown, envStringBaseUrl, urlApiLogin, urlApiUsers, requestPromise, insertFakeUsers, insertUser) {
  return function(test) {
    return pgDropCreateMigrate().bind({}).then(function() {
      return insertUser({
        email: 'test@example.com',
        name: 'exampleuser',
        password: 'topsecret',
        rights: 'canAccessCockpit'
      });
    }).then(function() {
      return command_serve('cockpit');
    }).then(function() {
      return requestPromise({
        method: 'POST',
        url: envStringBaseUrl + urlApiLogin(),
        json: true,
        body: {
          username: 'exampleuser',
          password: 'topsecret'
        }
      });
    }).then(function(arg) {
      var response;
      response = arg[0];
      test.equal(response.statusCode, 200);
      this.token = response.body.token;
      return requestPromise({
        method: 'POST',
        url: envStringBaseUrl + urlApiUsers(),
        headers: {
          authorization: "Bearer " + this.token
        },
        json: true
      });
    }).then(function(arg) {
      var response;
      response = arg[0];
      test.equal(response.statusCode, 403);
      return shutdown();
    }).then(function() {
      return test.done();
    });
  };
};

module.exports.testApiUsersPostUnprocessable = function(command_serve, pgDropCreateMigrate, shutdown, envStringBaseUrl, urlApiLogin, urlApiUsers, requestPromise, insertFakeUsers, insertUser) {
  return function(test) {
    return pgDropCreateMigrate().bind({}).then(function() {
      return insertUser({
        email: 'test@example.com',
        name: 'exampleuser',
        password: 'topsecret',
        rights: 'canAccessCockpit\ncanCreateUsers'
      });
    }).then(function() {
      return command_serve('cockpit');
    }).then(function() {
      return requestPromise({
        method: 'POST',
        url: envStringBaseUrl + urlApiLogin(),
        json: true,
        body: {
          username: 'exampleuser',
          password: 'topsecret'
        }
      });
    }).then(function(arg) {
      var response;
      response = arg[0];
      test.equal(response.statusCode, 200);
      this.token = response.body.token;
      return requestPromise({
        method: 'POST',
        url: envStringBaseUrl + urlApiUsers(),
        body: {
          email: 'dkjdlkf',
          name: '',
          password: ''
        },
        headers: {
          authorization: "Bearer " + this.token
        },
        json: true
      });
    }).then(function(arg) {
      var response;
      response = arg[0];
      test.equal(response.statusCode, 422);
      test.deepEqual(response.body, {
        name: 'must not be the empty string',
        password: 'must not be the empty string',
        email: 'must be an email address',
        rights: 'must not be null or undefined'
      });
      return shutdown();
    }).then(function() {
      return test.done();
    });
  };
};

module.exports.testApiUsersPostUnprocessableTaken = function(command_serve, pgDropCreateMigrate, shutdown, envStringBaseUrl, urlApiLogin, urlApiUsers, requestPromise, insertFakeUsers, insertUser) {
  return function(test) {
    return pgDropCreateMigrate().bind({}).then(function() {
      return insertUser({
        email: 'test@example.com',
        name: 'exampleuser',
        password: 'topsecret',
        rights: 'canAccessCockpit\ncanCreateUsers'
      });
    }).then(function() {
      return command_serve('cockpit');
    }).then(function() {
      return requestPromise({
        method: 'POST',
        url: envStringBaseUrl + urlApiLogin(),
        json: true,
        body: {
          username: 'exampleuser',
          password: 'topsecret'
        }
      });
    }).then(function(arg) {
      var response;
      response = arg[0];
      test.equal(response.statusCode, 200);
      this.token = response.body.token;
      return requestPromise({
        method: 'POST',
        url: envStringBaseUrl + urlApiUsers(),
        body: {
          email: 'test@example.com',
          name: 'user',
          password: 'opensesame',
          rights: ''
        },
        headers: {
          authorization: "Bearer " + this.token
        },
        json: true
      });
    }).then(function(arg) {
      var response;
      response = arg[0];
      test.equal(response.statusCode, 422);
      test.deepEqual(response.body, {
        email: 'taken'
      });
      return requestPromise({
        method: 'POST',
        url: envStringBaseUrl + urlApiUsers(),
        body: {
          email: 'user@example.com',
          name: 'exampleuser',
          password: 'opensesame',
          rights: ''
        },
        headers: {
          authorization: "Bearer " + this.token
        },
        json: true
      });
    }).then(function(arg) {
      var response;
      response = arg[0];
      test.equal(response.statusCode, 422);
      test.deepEqual(response.body, {
        name: 'taken'
      });
      return requestPromise({
        method: 'POST',
        url: envStringBaseUrl + urlApiUsers(),
        body: {
          email: 'test@example.com',
          name: 'exampleuser',
          password: 'opensesame',
          rights: ''
        },
        headers: {
          authorization: "Bearer " + this.token
        },
        json: true
      });
    }).then(function(arg) {
      var response;
      response = arg[0];
      test.equal(response.statusCode, 422);
      test.deepEqual(response.body, {
        email: 'taken',
        name: 'taken'
      });
      return shutdown();
    }).then(function() {
      return test.done();
    });
  };
};

module.exports.testApiUsersPostOk = function(command_serve, pgDropCreateMigrate, shutdown, envStringBaseUrl, urlApiLogin, urlApiUsers, requestPromise, insertFakeUsers, insertUser) {
  return function(test) {
    return pgDropCreateMigrate().bind({}).then(function() {
      return insertUser({
        email: 'test@example.com',
        name: 'exampleuser',
        password: 'topsecret',
        rights: 'canAccessCockpit\ncanCreateUsers'
      });
    }).then(function() {
      return command_serve('cockpit');
    }).then(function() {
      return requestPromise({
        method: 'POST',
        url: envStringBaseUrl + urlApiLogin(),
        json: true,
        body: {
          username: 'exampleuser',
          password: 'topsecret'
        }
      });
    }).then(function(arg) {
      var response;
      response = arg[0];
      test.equal(response.statusCode, 200);
      this.token = response.body.token;
      return requestPromise({
        method: 'POST',
        url: envStringBaseUrl + urlApiUsers(),
        body: {
          email: 'user@example.com',
          name: 'user',
          password: 'opensesame',
          rights: ''
        },
        headers: {
          authorization: "Bearer " + this.token
        },
        json: true
      });
    }).then(function(arg) {
      var response;
      response = arg[0];
      test.equal(response.statusCode, 201);
      test.equal(response.body.email, 'user@example.com');
      test.equal(response.body.name, 'user');
      test.equal(response.headers.location, urlApiUsers(response.body.id));
      return shutdown();
    }).then(function() {
      return test.done();
    });
  };
};
